const ApplicationEvent = ({title, description, id}) => (
        <div>
            <h3>{title}</h3>
            <p>{description}</p>
            <br />
        </div>
)

export default ApplicationEvent;
